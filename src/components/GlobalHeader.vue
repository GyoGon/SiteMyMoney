  
<script setup>
// Importa los componentes necesarios de Vue Router
import { useRoute } from 'vue-router'

// Define el array de navegación con rutas y nombres de componente
const navigation = [
  { name: 'Home', path: '/' },
  { name: 'Account', path: '/account' },
  { name: 'User', path: '/user' }
]

// Obtiene la ruta activa actual
const route = useRoute()
</script>

<template>
<div class="bg-white">
  <header class="absolute inset-x-0 top-0 z-50">
    <nav class="bg-white border-gray-200 dark:bg-gray-900">
      <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
        <a href="#" class="flex items-center space-x-3 rtl:space-x-reverse">
          <img src="../assets/logo.png" class="h-8" alt="Flowbite Logo" />
          <span class="self-center text-2xl font-semibold whitespace-nowrap dark:text-white">MyMoney</span>
        </a>
        <div class="hidden w-full md:block md:w-auto" id="navbar-default">
          <ul class="font-medium flex flex-col p-4 md:p-0 mt-4 border border-gray-100 rounded-lg bg-gray-50 md:flex-row md:space-x-8 rtl:space-x-reverse md:mt-0 md:border-0 md:bg-white dark:bg-gray-800 md:dark:bg-gray-900 dark:border-gray-700">
            <!-- Itera sobre la array de navegación y crea enlaces de router -->
            <li v-for="item in navigation" :key="item.name">
              <router-link :to="item.path" class="block py-2 px-3 text-gray-900 rounded hover:bg-gray-100 md:hover:bg-transparent md:border-0 md:hover:text-blue-700 md:p-0 dark:text-white md:dark:hover:text-blue-500 dark:hover:bg-gray-700 dark:hover:text-white md:dark:hover:bg-transparent" :class="{ 'font-bold': route.path === item.path }">
                {{ item.name }}
              </router-link>
            </li>
          </ul>
        </div>
      </div>
    </nav>
  </header>
</div>
</template>